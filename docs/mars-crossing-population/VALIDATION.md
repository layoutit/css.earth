# Mars-crossing validation

The five original closed DAMIT meshes retain their exact bytes, source coordinates and connectivity anchors. Independent signed-volume and scale checks, scientific sampler comparisons, decoded atlas probes and generic package/source closure pass for every body. Shadows and Orbit default off.

| Body | Prepared faces | Sampled original → prepared maximum | Sampled prepared → original maximum |
| --- | --- | --- | --- |
| Aethra | 800 | 146.24 m | 175.32 m |
| Lyyli | 800 | 27.98 m | 32.32 m |
| Hela | 800 | 57.48 m | 60.02 m |
| Kemi | 800 | 61.98 m | 70.03 m |
| Taurinensis | 800 | 51.19 m | 59.06 m |

Each surface comparison samples 8,192 deterministic area-stratified points in each direction. These are measured sampled distances, not an exhaustive Hausdorff bound or an estimate of the scientific model's accuracy. Four orthographic source/result views per body are retained locally; selected comparisons are included in `evidence/`. Their two halves are independently framed shape inspections, not browser pixel-parity images. The largest scalar difference from the independent NumPy projection is below 3.2e-13 km.

The existing marker composer preserves all 408 main-branch tiles before adding five. Current-main comet2p/209p packages and Sun destinations remain present. The serial transport refresh asserts that every non-marker runtime field remains identical; generic page metadata and Sun context are finalized afterward. The shared minimap includes the current 413-object registry and passes its five focused tests.

Both focused Horizons tests pass, including measured endpoint guards for the five additions. The maximum fitted-epoch difference is below 1 mm. The ±30-day endpoints measure fixed-conic propagation errors up to 3,627.75 km (Hela); these fixtures do not establish intervening-date or long-term accuracy.

Delivery: 175 selected assets were published and installed into an empty destination with one transfer at a time. All 38,803,094 downloaded bytes match their expected sizes and SHA-256 values; zero files were reused.

The final production build, actual DPR 1/2 browser views, shared navigation checks and one retained drag remain pending. No all-body browser sweep or physical-device performance claim is made.
