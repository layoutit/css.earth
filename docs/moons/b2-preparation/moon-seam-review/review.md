The Moon captures do not establish a new B2 seam regression. The complete prepared body, all 452 retained leaves, camera and geometry recipe match the pre-B2 `98ef3269` baseline. All 12 visible-color, GRAIL crust and curvature assets also match both the baseline manifest and their actual current file hashes. The inspected projective adapter and style paths have no B2 delta. Exact current HEAD is `7030be3b850734d73688cff595f12983234f42b2`; `evidence.json` records capture and asset hashes.

There is one concrete inherited coordinate limitation that can contribute to the visible face grid. `tools/objects/static-surface/band-scene.mjs:70` explicitly keeps texture overlap zero for legacy oriented bands; lines 104–108 expand geometry corners, while lines 121–127 retain the nominal source rectangle. Moon's overlap is 0.008 and each cell spans 11.25 degrees in both axes. A geometric cell therefore spans 11.43 degrees but displays a nominal 11.25-degree source patch. The independent endpoint check in `check.py` confirms each edge-corner coordinate is displaced by 0.09 degrees (about 2.73 km in latitude at the adopted radius). This check does not assert a browser-visible crack width or exact interpolation behavior inside a planar face.

This limitation already exists for normal imagery. Merely expanding the source rectangle is incorrect: `tools/objects/static-surface/projection.mjs:1` reverses rows separately within each latitude band, so reading beyond an existing band's edge reads the wrong geographic row. A proper correction requires packing adjoining source rows into explicit band gutters and registering the expanded geometric footprint to those pixels, or a new per-face source mapping contract. All lenses currently share the same prepared leaf geometry and background addresses. Changing that contract would affect the baseline photographic path too. No such patch is proposed or applied within B2.

The gray polar cap and orderly bright grid in the rock-abundance screenshot are expected missing-data cartography, not seams to remove. `src/platform/prepare-missing-coverage.mjs:43` paints 10-degree parallels and 30-degree meridians into missing cells. The retained small polar atlas visibly contains that same pattern, and the source coverage is limited to ±60 degrees. Both scientific polar assets have binary alpha in their current decoded outputs, as intended by nearest/lossless preparation. Visible-color poles retain their unchanged baseline antialiased alpha. Nonzero RGB under transparent pixels in lossless WebP is not an opaque-data defect.

The screenshots also contain fine irregular lines in normal and elevation views. Their remaining contribution from CSS rasterization, polygon overlap or polar joins cannot be separated from source mapping by these read-only checks. There is no controlled baseline browser capture in this review, so this report does not label every line a mapping defect or claim that CSS seams are unavoidable. B2 can retain its numeric-source and nearest/lossless improvements with the inherited face/pole seam limitation explicit; it should not claim zero seams.

No checkout edits, source rebakes, browser runs or aggregate tests were performed. Only existing small encoded pole assets were decoded, and the bounded analytic coordinate check was run. Evidence: `/tmp/moon-seam-review/evidence.json`; reproducible read-only check: `/tmp/moon-seam-review/check.py`.
