# Phobos and Deimos navigation context reconciliation

Retain the canonical regenerated WebPs. This is correction of stale prepared navigation images, not a source change in B3.

Both committed context WebPs last changed at `0d1b7b1bb`; their pinned source PNGs changed later in `ef07fac2d` (PR 62). The current navigation recipes and source PNGs remain byte-identical to incoming main `b60f5d588` and match the declared byte counts/hashes. The unchanged generator reads those sources at the declared 512 px size and uses WebP quality 85 with alpha quality 100.

The regenerated images' alpha channels match their pinned source PNGs exactly. The previous Phobos/Deimos images differ from those source alpha channels at 9,385 / 4,368 pixels. Opaque RGB mean absolute error against the PNG falls from 12.29 to 2.63 byte levels for Phobos and 2.52 to 1.68 for Deimos; residual RGB differences are consistent with the declared lossy display encoding. All four old/current images were visually inspected: the newer source silhouette and terrain are retained.

[Exact image and source hashes, dimensions and comparison metrics](phobos-deimos-context-review.json) preserve this read-only check. No image was regenerated by the reviewer and no checkout image, source or runtime was edited. Full browser qualification remains separate.
