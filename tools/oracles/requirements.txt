# Pinned oracle environment for tools/oracles/*/*.py, every package including
# transitive ones, created by `pnpm oracles:setup` under .local/oracles/venv.
# Oracles verify preparation results against reference implementations; they
# never produce pipeline inputs. Fixture tool versions must match these pins.
astropy==8.0.1
astropy-iers-data==0.2026.9.7.0.56.14
numpy==2.5.3
packaging==26.3
pds4_tools==1.4
pvl==1.3.2
pyerfa==2.0.1.5
PyYAML==6.0.3
spiceypy==8.2.0
