// `@cssearth/bake/environment`: the environment-image replay (Node only). Rebakes an environment object's missing
// runtime images from its pinned recipe through the star, shell, density-volume and image-layer bakes, and checks them.
export * from './environment-images.ts';
