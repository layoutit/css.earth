# The ALMA archive cuts this window out of its own image on request and has returned the same bytes each time, but a change to
# that service could alter the file, so the 325 KB the sidebar picture is drawn from are kept here.
!/source/alma/haumea-band7-continuum-cutout.fits
