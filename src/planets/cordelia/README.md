# Cordelia

Uranus’s inner epsilon-ring shepherd, represented by its published prolate shape fit and the standard no-coverage grid. Uses the generic object contract, native raster triangles and shared lighting. See SOURCE.md for measurements and interpretation.

`pnpm setup:assets --object=cordelia` installs the published runtime assets.
