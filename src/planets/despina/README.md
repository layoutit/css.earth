# Despina

A measured ellipsoid with the standard no-coverage grid, through the generic
object contract. Shape model is the only dataset; see SOURCE.md for the image
survey and limits. Shared flood lighting and Shadows are supported.

`pnpm setup:assets --object=despina` installs prepared runtime assets.
