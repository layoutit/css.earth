# Puck

Source-registered Voyager monochrome observation with explicit missing coverage on an approximate reference sphere. Uses the generic object contract and shared lighting. See SOURCE.md.

`pnpm setup:assets --object=puck` installs published runtime assets.
