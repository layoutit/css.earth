# Phobos

Authored body package consumed by the shared object adapter. Preparation owns the sourced shape, surface raster, lighting and navigation images.

`pnpm setup:assets --object=phobos` installs the published runtime assets. Source acquisition and preparation use the common tools.
