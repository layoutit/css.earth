# Portia

Published prolate shape fit with the standard no-coverage grid. Uses the generic object contract, native raster triangles and shared lighting. See SOURCE.md for measurements and interpretation.

`pnpm setup:assets --object=portia` installs the published runtime assets.
