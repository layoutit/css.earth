# Thalassa

Measured flattened ellipsoid with the shared no-coverage grid. See SOURCE.md for selected dimensions and image qualification.

`pnpm setup:assets --object=thalassa` installs the prepared package.
