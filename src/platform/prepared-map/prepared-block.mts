// The checked TypeScript owner is shared with the renderer.
export { PREPARED_BLOCK_SCHEMA, PREPARED_BLOCK_LIMITS, createPreparedBlockDecoder, decodePreparedBlock, decodePreparedBlockAsync, restoreWmtsRecords } from "../../renderers/css/dist/platform/prepared-block.js";
