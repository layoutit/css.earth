/** Explicit cssEarth host integration; implementation remains with its source owner. */
export { compileCssVolume } from '@cssearth/bake/volume-leaves';
export type { PreparedCssVolume } from '@cssearth/bake/volume-leaves';
export { CSS_COMPILER_RENDER_BUDGET } from '@cssearth/renderer/volume/compiler-render-budget.ts';
