/** Explicit host boundary; source data and implementation remain with their canonical owner. */
export { encodeFits, decodeFits, float32LittleEndian, type FitsValue } from '../../../../../../../tools/nebula/application/fits.ts';
