/** Explicit host boundary; calibrated survey band composition remains with its canonical owner. */
export { composeSkyBandPng } from '../../../../../../../tools/objects/observation/sky-band-composite.mts';
