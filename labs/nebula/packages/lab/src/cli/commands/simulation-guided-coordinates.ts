/** Relocated to `@cssearth/bake/volume` so replay and the lab share one observer-ray convention. */
export { ARCSECONDS_PER_RADIAN, angularScale, fieldToPhysical, physicalBounds, physicalToField } from '@cssearth/bake/volume';
