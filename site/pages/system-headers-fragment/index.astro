---
// Every planetary system's card header, rendered once for all pages. A page carries only the Solar System's and its
// own; the first overview of another system fetches the rest (`site/system-headers-fragment.mts`).
import SystemHeaders from '../../components/SystemHeaders.astro';
---
<div data-system-headers><SystemHeaders /></div>
