---
import ObjectPage from '../components/ObjectPage.astro';
import { SCENE_OBJECTS } from '../objects.mts';
export function getStaticPaths() {
  return SCENE_OBJECTS.map(({ id }) => ({ params: { id } }));
}
---
<ObjectPage objectId={Astro.params.id!} />
