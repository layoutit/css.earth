---
import type { HTMLAttributes } from 'astro/types';
interface Props extends HTMLAttributes<'div'> {}
const { class: className, ...attributes } = Astro.props;
---
<div class:list={['object-card', className]} {...attributes}>
  <slot />
</div>
