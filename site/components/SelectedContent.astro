---
interface Props { title: string; metadata?: string | null; lead?: string; description?: string; }
const { title, metadata, lead, description } = Astro.props;
---
<slot name="header">
  <h3 class="planet-lens-details-title">{title}{metadata && <span class="planet-selected-content-meta"> ({metadata})</span>}</h3>
</slot>
<slot />
<p class="planet-lens-details-copy">{lead && <><strong class="planet-lens-details-lead">{lead}.</strong> </>}{description}</p>
