---
interface Props { title: string; metadata?: string | null; lead?: string; description?: string; descriptionDetail?: string; }
const { title, metadata, lead, description, descriptionDetail } = Astro.props;
---
<slot name="header">
  <h3 class="planet-lens-details-title">{title}{metadata && <span class="planet-selected-content-meta"> ({metadata})</span>}</h3>
</slot>
<slot />
<p class="planet-lens-details-copy" title={descriptionDetail}>{lead && <><strong class="planet-lens-details-lead">{lead}.</strong> </>}{description}</p>
