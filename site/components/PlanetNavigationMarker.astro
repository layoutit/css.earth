---
import "../planet-navigation-marker.css";
import { PREPARED_NAVIGATION_MARKERS } from "../prepared-navigation-markers.mjs";
import { markerStyle } from "../../src/navigation/marker-presentation.mjs";

interface Props {
  planetId: string;
  color: string;
  index: number;
  count: number;
  scale?: number;
}

const { planetId, color, index, count, scale = 1 } = Astro.props;
const { ringed, style } = markerStyle(PREPARED_NAVIGATION_MARKERS[planetId], { color, scale });
---

<span class:list={["planet-navigation-marker", planetId, { ringed }]} style={style} aria-hidden="true">
  <i></i>
</span>
