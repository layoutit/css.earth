---
import type { HTMLAttributes } from 'astro/types';
interface Props extends Omit<HTMLAttributes<'details'>, 'title'> { title: string; count?: number; }
const { title, count, open = false, class: className, ...attributes } = Astro.props;
---
<details class:list={['planet-accordion', className]} open={open} {...attributes}>
  <summary class="planet-panel-summary">
    <h2 class="planet-panel-heading">{title}{count !== undefined && <> <span class="planet-panel-heading-count">({count})</span></>}</h2>
    <span class="planet-panel-toggle" aria-hidden="true"></span>
  </summary>
  <slot />
</details>
